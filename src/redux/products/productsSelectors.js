export const laptopsSelector = (state) => 
    state.products.items.laptops;

export const phonesSelector = (state) => 
    state.products.items.phones;

export const productsLoaderSelector = (state) => 
    state.products.loader;

    