import React from 'react';
import Section from '../components/Section';

const AuthPage = () => {
    return (
        <Section>
            <h1>Auth Page</h1>
        </Section>
    );
};

export default AuthPage;