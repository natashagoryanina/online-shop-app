import React from 'react';
import Section from '../components/Section';

const ProfilePage = () => {
    return (
        <Section>
            <h1> Profile</h1>
        </Section>
    );
};

export default ProfilePage;