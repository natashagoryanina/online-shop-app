import React from 'react';
import Section from '../components/Section';

const HomePage = () => {
    return (
        <Section>
            <h1> Home Page</h1>
        </Section>
    );
};

export default HomePage;